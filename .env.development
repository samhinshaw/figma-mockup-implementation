# Committed, non-secret dev defaults. Vite auto-loads this in `pnpm dev`.
# Turns on the in-browser MSW mock backend so the app has data with no server.
# Override locally in `.env.local` (gitignored), e.g. to point at a real API.
VITE_ENABLE_MOCKS=true
