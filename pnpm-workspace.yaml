# pnpm settings. Approve dependency build scripts here (pnpm 10+ blocks them by
# default). esbuild ships its binary via a platform package, so its build script
# is optional, but approving it keeps installs warning-free.
onlyBuiltDependencies:
  - esbuild
